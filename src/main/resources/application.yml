spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pricewatcher
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  jackson:
    serialization:
      write-dates-as-timestamps: false

twilio:
  account-sid: ${TWILIO_ACCOUNT_SID}
  auth-token: ${TWILIO_AUTH_TOKEN}
  from-whatsapp: "whatsapp:+5521983283418"
  from-sms: "+5521983283418"

logging.level.org.springframework: INFO
server.port: 8080